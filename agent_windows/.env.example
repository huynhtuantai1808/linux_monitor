# Windows Agent .env
# Set MASTER_URL to your Master Server IP and port
MASTER_URL=http://<IP_MASTER>:8000/metrics
INTERVAL=60

# Optional: Monitor specific drives (comma-separated), leave blank for all drives
# Example: MONITOR_DRIVES=C:,D:
MONITOR_DRIVES=
